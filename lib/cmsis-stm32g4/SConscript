Import("env")
Import("conf")
Import("objs")

if conf["LIB_CMSIS_STM32G4"] == "y":

	env.Git('https://github.com/STMicroelectronics/cmsis-device-g4.git', branch='master')

	env.Append(CPPPATH = [
		Dir(env['LIB_DEFAULT_REPO_DIR'] + '/Include'),
	])
